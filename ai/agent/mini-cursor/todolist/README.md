# TodoList

基于 React + Vite + TypeScript 的待办列表应用。

## 功能

- 添加待办
- 勾选完成 / 取消完成
- 删除待办
- 筛选：全部 / 未完成 / 已完成
- 显示剩余未完成数量

## 运行

```bash
# 安装依赖
pnpm install
# 或 npm install

# 开发
pnpm dev
# 或 npm run dev

# 构建
pnpm build
# 或 npm run build
```

开发时访问终端提示的本地地址（一般为 http://localhost:5173）。
